func main()
begin
    var a <- "simp"
    for i until not (simp + 4 > beo(i) / unu(i)) by print("simp")...print("simp2") 
    begin
        if (a = 1) continue
        else begin
            for j until j < main() by -1 begin
                simp[beo(i), j] <- [i, j]
            end
        end
        
    end
    return
end
        